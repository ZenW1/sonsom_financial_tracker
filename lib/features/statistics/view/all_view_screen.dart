import 'package:flutter/material.dart';

class AllViewScreen extends StatelessWidget {
  const AllViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
